/*
* Stylesheet travel page
*/

import { StyleSheet } from 'react-native';

const travel = StyleSheet.create({

    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    title: {
        flex: 1,
        fontSize: 50,
        alignItems: 'center',
        justifyContent: 'center',
    },

});

export default travel;
